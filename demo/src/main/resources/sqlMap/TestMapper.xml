<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.BshanYey.dao.TestMapper" >
   

  <select id="findAll" resultType="String">
  	select mrid from patient
  </select>
  
  <insert id="addSF" parameterType="com.hyhl.mzsf.po.SFExample" useGeneratedKeys="true" keyProperty="id">
<!--   	<selectKey keyProperty="id" order="AFTER" resultType="int"> -->
<!-- 		SELECT LAST_INSERT_ID() -->
<!-- 	</selectKey> -->
  	INSERT INTO sf (name,num,type,mrid,patientid) VALUES (#{name},#{num},#{type},#{mrid}, #{patientid});
  </insert>

</mapper>